-*- mode: org -*-

- add --attach flag to send output as an attachment instead of as the body of
  the email.  or maybe --email-attach?  whatever works better.  keeping the
  status/summary as the body of the email would be super, too.
